import { NightSite } from "@/components/looks/night-site";

export const metadata = {
  title: "Night market look preview — The CF Events Team",
};

export default function NightMarketLookPage() {
  return <NightSite />;
}
