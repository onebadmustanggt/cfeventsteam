import { HipSite } from "@/components/looks/hip-site";

export const metadata = {
  title: "Hip look preview — The CF Events Team",
};

export default function HipLookPage() {
  return <HipSite />;
}
